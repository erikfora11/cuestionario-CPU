# cuestionario-CPU

## Leaderboard

La tabla de mejores puntajes se guarda en `leaderboard.json`.

Para iniciar el juego con persistencia en JSON:

```bash
node server.js
```

Abre http://localhost:3000, termina una partida, escribe tu nombre y pulsa **Guardar**.